[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: tests unitarios sin I/O externo
    integration: tests de integración con SQLite temporal
    e2e: tests con browser Playwright real
